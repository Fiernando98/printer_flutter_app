name: printer_flutter_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1.0.0

environment:
  sdk: ">=2.17.0-87.0.dev <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
  image_picker:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true